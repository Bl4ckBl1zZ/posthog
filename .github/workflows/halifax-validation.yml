# Managed by Halifax. Edit pool routing in Halifax, then use Repair to refresh this file.
name: Halifax validation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: halifax-bl4ckbl1zz-posthog-6c1d8cbb
    timeout-minutes: 15
    steps:
      - name: Verify Halifax runner tools
        run: |
          node --version
          docker info
          uname -a
